<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a434_health_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Nutrition_Goal"></activity>
        <activity android:name=".Physical_Goal" />
        <activity android:name=".Mental_Goal" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Checkin_Activity" />
        <activity android:name=".Mental_Checkin" />
        <activity android:name=".Profile_Activity" />
        <activity
            android:name=".Goals_Activity"
            android:label="@string/title_activity_goals_" />
        <activity
            android:name=".activity_types_of_goals"
            android:label="activity_types_of_goals" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>